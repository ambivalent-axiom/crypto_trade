<!DOCTYPE html>
<html>
<head>
    <title>Top Currencies</title>
</head>
<body>
<h1>Currency List</h1>
<table border="0">
    <thead>
    <tr>
        <th>Name</th>
        <th>Symbol</th>
        <th>Price</th>
    </tr>
    </thead>
    <tbody>
    {% for item in items %}
        <tr>
            <td>{{ item.getName() }}</td>
            <td>{{ item.getSymbol() }}</td>
            <td>{{ item.getPrice() }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
</body>
</html>