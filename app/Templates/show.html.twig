{% include 'header.html.twig' %}
<body>
<h1>Search results</h1>
<table border="0">
    <thead>
    <tr>
        <th>Name</th>
        <th>Symbol</th>
        <th>Price</th>
    </tr>
    </thead>
    <tbody>
    {% for item in items %}
        <tr>
            <td>{{ item.getName() }}</td>
            <td>{{ item.getSymbol() }}</td>
            <td>{{ item.getPrice() }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
</body>
</html>